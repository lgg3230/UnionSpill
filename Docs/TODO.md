# UnionSpill — To-Do List

## Data & Pipeline

- [ ] **Audit the flows pipeline.** Understand the full construction of `totalflows_pw`, `outflows_pw`, `inflows_pw` (and their raw count counterparts `totalflows`, `outflows`, `inflows`) from the bilateral connectivity CSVs through `011b_totalflows_panel.py` → `totalflows_panel_2009_2016.csv`. Clarify the exact denominator used for the `_pw` normalization and verify consistency with the `totalflows_wide_2007_2011.csv` used for pre-treatment controls.

## Analysis

## Writing
